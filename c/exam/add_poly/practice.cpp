
//store from the front
TITEM *res = NULL;
TITEM **lastptr = &res;

*lastptr = createItem

lastptr = &((*lastptr)->m_Next)

//store for the end
TITEM * tail = NULL;
while(input is used)
tail=createItem(m_Digit, tail);

//
while(1)
{
    //resize

    if(scanf1 == valid)
    {
        //store and increment
        continue;
    }
    if(scanf2 == valid)
    {
        //store adn incremeetr
        continue
    }
    else 
        //break
}
///////////////////


-->check the first element,--> read until it is certain datatype
bool readNumber(char ** buf, int *num)
{
    if(!isDigit(*(*buf))) 
    {
        return false;
    }
    while(sscanf(*buf, "%d", &num[i]) && i<20)
    {
        i++;
        (*buf)++;
    }
    return true;
}

bool readName(char** buf, Name * name)//space alllowed
{
    if(!Alpha(*(*buf)))
    {`
    }
    while(Alpha || space)
    {

    }
    //checks the last element
}

bool readSpace(char ** buf)
{
    if(*(*buf) != ' ')
        return false;

    while(*(*buf) == ' ')
    {
        (*buf)++;
    }

    return true;
}

