#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>


for(size_t i=0;i<arr->len;i++)
{
    printf("%d\n", arr->data[i]);
}

for(size_t=arr->len;i<arr->capa;i++)
{
    arr->data[i]={0};
}