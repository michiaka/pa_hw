#include <stdio.h> 
#include <stdlib.h> 
#include <string.h> 
#include <assert.h> 
#include <ctype.h>

int main () {
    int* array[10];
    // for(int i = 0; i < 3 ; i++){
    // printf("num%d: \n", i+1);
    // scanf("%lf", &array[i]);}
    array[0] ="";

    if(array[0])
    return 0;

}