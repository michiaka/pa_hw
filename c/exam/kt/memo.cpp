#include<stdio.h>
#include<stdlib.h>

const char * foo(const char  *str)
{
    while(*str++){}
    return str;
}
int main()
{
    const char *str = "PA1 quiz";
    printf("%d\n", *foo(str));

    return 0;
}


for(i = 1; i < n; i *=2)
    for(j = 0; j < i; j++)
        doIt();

//the sum of the each inner-loop itertation

assigning the char into int will work without any problem


a=b=c //not work

a&&b&&c //work

//--------------UTFP
-simple_one
UCS-2
UTF-32
//---------------

int a[3];
int *b = a;


double d;
int cnt =0;
for(d=0;d!=13;d+=0.2)//when working with float number, it is not good idea to use comparison 
cnt++;
printf("%d", cnt):
return 0;