#include <stdio.h>



int main (){

    Prntf("Insert the number in this format 2 times
    , identifier [ number, number, number, number ]:\n");
    /*storing into two arrays*/

    for(int i = 0; i >= 3 ; i++ ){
    if((a[i]!=b[0] && a[i]!=b[1] && a[i]!=b[2] && a[i]!=[3]) )
    {
        printf("Two strings are different.\n");
        return 0;
    }
    
    }
}
